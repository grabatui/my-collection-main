{% extends 'layout/email.twig' %}

{% block body %}
    <p>Здравствуйте, {{ userName }}!</p>

    <p>На данный адрес был отправлен запрос на смену пароля. Если это были Вы, нажмите на ссылку ниже и смените пароль. Если это было сделано без Вашего ведома, обратитесь в службу поддержки.</p>

    <table role="presentation" border="0" cellpadding="0" cellspacing="0" class="btn btn-primary">
        <tbody>
        <tr>
            <td align="left">
                <table role="presentation" border="0" cellpadding="0" cellspacing="0">
                    <tbody>
                    <tr>
                        <td> <a href="{{ currentDomain }}/reset-password/{{ resetToken }}" target="_blank">Сменить пароль</a> </td>
                    </tr>
                    </tbody>
                </table>
            </td>
        </tr>
        </tbody>
    </table>
{% endblock %}